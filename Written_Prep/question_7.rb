# When can you pass a block to a method? Why?

=begin
In Ruby, all methods can take an optional block as an implicit argument.
However, if a method doesn't yield to a block, the code within the block will
not be executed.
=end
